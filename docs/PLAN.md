# План-график разработки проекта  

---

## Спринт 0. Подготовка и инфраструктура  
**Ориентировочные даты:** 14.06 – 21.06  
**Соответствует ДЗ:** нет (перед первым ДЗ)  
**Цель:** Создать репозиторий, структуру, базовый docker-compose для всей инфраструктуры и один сервис-заглушку.

**Задачи:**

- [x] Создать монорепозиторий на GitHub, настроить доступы.
- [x] Подготовить структуру каталогов (монорепо, каталоги по мере необходимости):
  - `services/<name>/` — микросервисы (cmd, internal, … внутри сервиса)
  - `api/` — proto-файлы, Swagger-спецификации
  - `docs/` — дизайн-документ, ADR, план
- [x] Написать `docker-compose.yml` для инфраструктуры *(частично — PG, Redis, NATS в спринте 2; полный стек — позже)*:
  - PostgreSQL, MongoDB, Redis, NATS (с JetStream), MinIO, Loki, Prometheus, Grafana.
- [ ] Добавить инициализацию MinIO *(отложено вместе с compose)* (создание бакета через init-container).
- [x] Создать первый микросервис-заглушку `order-service` с одним эндпоинтом `/health`.
- [x] Написать базовый `README.md` с описанием проекта, архитектуры и инструкцией по запуску.
- [x] Подготовить `Taskfile.yml` для локальной разработки (lint, test, build, run).

---

### Спринт 1. CI/CD, сборка и первый релиз  
**Ориентировочные даты:** 22.06 – 28.06  
**Соответствует ДЗ:** «Собираем приложение» (срок до 22.06, можно сдать до конца недели)  
**Цель:** Настроить пайплайны, линтеры, тесты и контейнеризацию для `order-service`.

**Задачи (обязательные):**

- [x] Дописать `order-service`: реализовать REST-эндпоинты (заглушки, например, создание заказа с валидацией).
- [x] Написать юнит-тесты для бизнес-логики (без внешних зависимостей).
- [x] Настроить `golangci-lint` и добавить проверку в CI.
- [x] Написать `Dockerfile` для order-service (multistage).
- [x] Настроить GitHub Actions (или другой CI):
  - этап `lint`
  - этап `test` (unit)
  - этап `build` (бинарник)
  - этап `docker-build` и `push` в Docker Hub (или GHCR).
- [x] Убедиться, что контейнер запускается локально и в CI.
- [x] Подготовить отчёт (скриншоты пайплайнов, запущенный контейнер).

**Опционально («звёздочка»):**

- [ ] Настроить автоматический деплой на свой VPS (через SSH или ArgoCD в будущем).

---

### Спринт 2. Тестирование (unit, интеграционные, нагрузочные)  
**Ориентировочные даты:** 29.06 – 05.07  
**Соответствует ДЗ:** «Собираем микросервисную систему» (срок до 01.07, сдвиг допустим)  
**Цель:** Покрыть `order-service` тестами, добавить интеграционные и нагрузочные тесты.

**Задачи (обязательные):**

- [x] Подключить PostgreSQL, Redis, NATS к `order-service` (goose, caarlos0/env, slog).
- [x] Написать интеграционные тесты для `order-service` с использованием `testcontainers`:
  - поднять PostgreSQL, NATS, Redis в тестах.
  - проверить создание заказа и публикацию события.
- [x] Добавить интеграционные тесты в CI (запускать автоматически при PR или по расписанию).
- [x] Провести нагрузочное тестирование эндпоинта `POST /orders` (например, `wrk` или `vegeta`):
  - замерить RPS, latency (p50, p95).
  - сохранить результаты в отчёт.
- [x] Подготовить отчёт о нагрузочном тестировании в `docs/SPRINT2_REPORT.md` (методика, метрики, выводы).
- [x] Оптимизировать код, если нагрузочные тесты выявили узкие места *(не потребовалось — p95 < 10 ms)*.

**Опционально («звёздочка»):**

- [ ] Добавить E2E-тест, который поднимает все сервисы (пока заглушки) и проверяет сквозной сценарий.

---

### Спринт 3. Наблюдаемость: логи, метрики, трассировка  
**Ориентировочные даты:** 06.07 – 12.07  
**Соответствует ДЗ:** «Проверяем наблюдаемость собранной системы» (срок до 08.07)  
**Цель:** Интегрировать Loki, Prometheus, Jaeger в `order-service` и настроить дашборды.

**Задачи (обязательные):**

- [x] Внедрить структурированное логирование (JSON) с `slog` *(базовый slog — спринт 2; Loki — здесь)*.
- [x] Настроить отправку логов в Loki (через Promtail или прямой HTTP-пуш).
- [x] Добавить экспорт метрик Prometheus:
  - число запросов, ошибки, гистограмма времени ответа, количество активных заказов.
- [x] Настроить алертинг в Prometheus (например, если ошибок >5% за 1 минуту) и отправку в Telegram (через Alertmanager). *(Telegram заменён webhook-receiver для dev; конфиг Alertmanager готов к подключению Telegram.)*
- [x] Интегрировать OpenTelemetry для трассировки:
  - добавить spans для HTTP-обработчиков, вызовов БД, публикаций NATS.
  - настроить экспорт в Jaeger.
- [x] Создать в Grafana дашборды (логи, метрики, трейсы).
- [x] Написать отчёт с демонстрацией работы наблюдаемости (скриншоты дашбордов, алертов).

**Опционально («звёздочка»):**

- [x] Добавить сбор метрик с БД (PostgreSQL, MongoDB) и NATS, вывести их на отдельный дашборд. *(PostgreSQL + NATS; MongoDB — в Store Service, спринт 5.)*

---

### Спринт 4. Безопасность: JWT, TLS, mTLS, хеширование  
**Ориентировочные даты:** 13.07 – 19.07  
**Соответствует ДЗ:** «Добавляем защиту взаимодействия между сервисами» (срок до 20.07)  
**Цель:** Защитить REST API и подготовить инфраструктуру для защищённого общения между сервисами.

**Задачи (обязательные):**

- [x] Реализовать JWT-аутентификацию (можно использовать `golang-jwt`) для REST-эндпоинтов *(заменить заголовок `X-User-ID`)*.
- [x] Настроить TLS для REST-сервера (самоподписанный сертификат для разработки).
- [x] Добавить хеширование паролей (если есть пользователи) с bcrypt.
- [x] Сгенерировать сертификаты для mTLS (CA, сертификаты сервисов) и добавить их в контейнеры.
- [x] Обновить CI/CD, чтобы при сборке подтягивались сертификаты (или генерировались на лету).
- [x] Написать отчёт с описанием механизмов защиты и примерами запросов с JWT.

**Опционально («звёздочка»):**

- [ ] Интегрировать Keycloak вместо самописной JWT (поднять Keycloak в compose и настроить OIDC).

---

### Спринт 5. Реализация всех микросервисов и протоколов взаимодействия  
**Ориентировочные даты:** 20.07 – 03.08 (две недели, т.к. большой объём)  
**Соответствует ДЗ:** «Включаем взаимодействие между сервисами» (срок до 05.08)  
**Цель:** Разработать все 5 сервисов, реализовать REST, gRPC (включая стриминг), Swagger, и простой веб-клиент.

**Задачи (обязательные):**

- [ ] Разработать **Payment Service**:
  - gRPC-сервер с методами `Charge`, `Refund`.
  - Подключить PostgreSQL.
  - Публиковать события в NATS после успешных операций.
- [ ] Разработать **Store Service**:
  - NATS-воркер для команд (резерв, подтверждение, освобождение).
  - Хранение в MongoDB (агрегаты товаров, резервы).
- [ ] Разработать **Notification Service**:
  - NATS-воркер, который логирует уведомления (можно писать в Loki или просто в stdout).
- [ ] Разработать **Analytics Service**:
  - NATS-воркер, сохраняющий чеки в MinIO и агрегированные данные в PostgreSQL.
- [ ] В **Order Service**:
  - Дописать REST-эндпоинты (list orders, полный lifecycle статусов).
  - Сгенерировать Swagger-документацию через `swaggo` и подключить UI.
  - Реализовать gRPC-клиент для вызова Payment Service (синхронно).
  - Реализовать gRPC-сервер со стримингом `WatchOrderStatus` (пушит статусы клиенту).
  - Настроить NATS-consumers и переходы статусов Saga (RESERVED → PAID → CONFIRMED).
- [ ] Написать интеграционные тесты, покрывающие все бизнес-сценарии (успех, отказ склада, отмена заказа).
- [ ] Создать простой веб-клиент (HTML + JS или отдельный Go-клиент), который:
  - через REST создаёт заказ.
  - подключается по gRPC-стриму и отображает статусы в реальном времени.
- [ ] Настроить шаблонизатор для статусной страницы `/health` (например, показывать состояние сервисов).

**Опционально («звёздочка»):**

- [ ] Добавить прокси-сервис (как в задании со звёздочкой), который фиксирует метрики трафика и может вносить помехи.

---

### Спринт 6. Работа с БД и кэшированием (высокая доступность, паттерны)  
**Ориентировочные даты:** 04.08 – 16.08  
**Соответствует ДЗ:** «Подключаем БД и систему кэширования» (срок до 17.08)  
**Цель:** Обеспечить надёжную работу с PostgreSQL, MongoDB, Redis; реализовать Rate Limiter и Distributed Mutex.

**Задачи (обязательные):**

- [ ] Написать миграции для PostgreSQL (Order, Payment, Analytics) с использованием `goose`.
- [ ] Настроить репликацию PostgreSQL (например, два контейнера: master + replica) в docker-compose.
- [ ] Настроить репликацию MongoDB (минимум 3 узла для отказоустойчивости) в compose.
- [ ] Подключить Redis:
  - кэширование корзины пользователя, TTL ключей.
  - реализация Rate Limiter (например, 100 запросов/мин на пользователя).
  - реализация Distributed Mutex для критических секций (например, при резервировании товара).
- [ ] Добавить `GET /orders` (список заказов пользователя).
- [ ] Протестировать отказы узлов БД (остановить master PostgreSQL, реплику MongoDB) и проверить, как система реагирует (логи, метрики, доступность).
- [ ] Написать отчёт с результатами тестирования отказоустойчивости БД.

**Опционально («звёздочка»):**

- [ ] Добавить etcd для хранения конфигураций и подписки на изменения (например, динамическое изменение лимитов Rate Limiter).
- [ ] Сравнить производительность с Tarantool/Picodata (можно поднять отдельно и провести бенчмарк).

---

### Спринт 7. Брокер сообщений и распределённое хранилище (NATS, MinIO, Elasticsearch)  
**Ориентировочные даты:** 17.08 – 31.08 (две недели)  
**Соответствует ДЗ:** «Подключаем брокер сообщений и распределенную систему хранения» (срок до 02.09)  
**Цель:** Настроить надёжную доставку через NATS с JetStream, интеграцию с MinIO и, при необходимости, поисковый движок.

**Задачи (обязательные):**

- [ ] Настроить NATS с JetStream (сохранять сообщения на диске, durable-подписки).
- [ ] Внедрить **Transactional Outbox** для атомарной публикации событий при checkout.
- [ ] Протестировать NATS под нагрузкой (отправить тысячи событий) и при отказе одного узла (если кластер).
- [ ] Подключить MinIO для Analytics Service:
  - сохранять JSON-чек заказа в бакет.
  - реализовать получение чека по временной ссылке.
- [ ] Реализовать адаптер для поиска:
  - Если используете Elasticsearch – поднять его, создать индекс, написать индексатор (воркер, слушающий события) и REST-эндпоинт для поиска.
  - Если не хотите поднимать Elasticsearch – реализовать адаптер на основе PostgreSQL с полнотекстовым поиском и в отчёте пояснить, что это альтернатива (это будет принято, т.к. вы демонстрируете принцип).
- [ ] Провести нагрузочное тестирование всей системы (сквозное) и замерить время ответа.
- [ ] Написать отчёт с результатами тестирования брокера и хранилища.

**Опционально («звёздочка»):**

- [ ] Реализовать работу с S3 с тегами, частичной загрузкой, версионированием (MinIO поддерживает).
- [ ] Подключить Cassandra как альтернативу для хранения событий (можно поднять и сравнить с PostgreSQL).

---

### Спринт 8. Рефакторинг и отказоустойчивость  
**Ориентировочные даты:** 01.09 – 07.09  
**Соответствует ДЗ:** «Рефакторим систему под требования отказоустойчивости» (срок до 07.09)  
**Цель:** Анализ архитектуры, внедрение паттернов устойчивости, улучшение производительности.

**Задачи (обязательные):**

- [ ] Провести анализ текущей архитектуры на предмет узких мест (документировать в ADR).
- [ ] Внедрить **Circuit Breaker** для gRPC-вызовов (например, с помощью `gobreaker` или `sony/gobreaker`).
- [ ] Добавить **Retry** с exponential backoff и джиттером для NATS-подписчиков.
- [ ] Проверить, как система ведёт себя при падении Payment Service (должна возвращать ошибку без зависания).
- [ ] При необходимости реализовать CQRS (разделить Order Service на команды и запросы, например, выделить отдельный сервис для чтения, но можно ограничиться репликацией).
- [ ] Убедиться, что все тесты (unit, integration, e2e) проходят.
- [ ] Написать финальный отчёт о рефакторинге и результатах тестирования отказоустойчивости.

**Опционально («звёздочка»):**

- [ ] Внедрить Saga-координатор (например, с использованием Temporal) вместо хореографии (покажете два подхода).
- [ ] Провести стресс-тестирование с одновременным отключением нескольких сервисов и записать время восстановления.
